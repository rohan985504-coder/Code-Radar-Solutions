#include <stdio.h>

int main() {
    int m,n;
    scanf("%d %d",&m, &n);
    if((m>n)&&(n<0)||(a==0)&&(b==0)){
      printf("True");
    }
    else{
        printf("False");

    }  
    return 0;
}