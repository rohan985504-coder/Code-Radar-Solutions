#include <stdio.h>
#include <ctype.h>

int main() {
    char a;
    scanf("%c",&a);
    if(isupper(ch)){
        printf("Uppercase");
    }
    else(islower(ch));
    printf("Lowercase");
}